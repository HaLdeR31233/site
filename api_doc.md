# API Endpoints та Routes Документація

## DIM.RIA - Система управління нерухомістю

## 📋 Зміст

1. [Routes (Маршрути)](#routes-маршрути)
2. [API Endpoints](#api-endpoints)
3. [Зовнішні API](#зовнішні-api)
4. [Структура проекту](#структура-проекту)
5. [Приклади використання](#приклади-використання)

---

## Routes (Маршрути)

### Таблиця всіх routes

| Route | Метод | Опис | Файл шаблону | HTTP Status |
|-------|-------|------|--------------|-------------|
| `/` | GET | Головна сторінка сайту | `pages/home.php` | 200 |
| `/home` | GET | Головна сторінка (альтернативний route) | `pages/home.php` | 200 |
| `/login` | GET | Сторінка входу в систему | `pages/login.php` | 200 |
| `/register` | GET | Сторінка реєстрації користувача | `pages/login.php` | 200 |
| `/*` (неіснуючий route) | GET | Сторінка помилки 404 | `pages/404.php` | 404 |
| `/*` (помилка сервера) | GET | Сторінка помилки 500 | `pages/500.php` | 500 |

### Детальний опис routes

#### 1. Головна сторінка

**Route:** `/` або `/home`  
**Метод:** GET  
**Опис:** Відображає головну сторінку сайту з каталогом нерухомості, рейтингом ріелторів, забудовників та агенцій.

**Параметри:** Немає

**Відповідь:**
- HTML сторінка з контентом
- Статус: 200 OK

**Функціонал:**
- Пошук нерухомості
- Фільтрація об'єктів
- Порівняння фраз (використовує Set)
- Завантаження випадкових зображень
- Статистика нерухомості (Chart.js)

---

#### 2. Сторінка входу

**Route:** `/login`  
**Метод:** GET  
**Опис:** Сторінка для авторизації користувачів у системі.

**Параметри:** Немає

**Відповідь:**
- HTML форма входу
- Статус: 200 OK

**Форма містить:**
- Поле Login
- Поле Email
- Поле Phone
- Поле Password
- Чекбокс "Запам'ятати мене"
- Кнопки соціальної авторизації (Google, Facebook)

---

#### 3. Сторінка реєстрації

**Route:** `/register`  
**Метод:** GET  
**Опис:** Сторінка для реєстрації нових користувачів.

**Параметри:** Немає

**Відповідь:**
- HTML форма реєстрації
- Статус: 200 OK

**Примітка:** Використовує той самий шаблон, що й сторінка входу (`pages/login.php`), але з іншим заголовком.

---

#### 4. Сторінка помилки 404

**Route:** `/*` (будь-який неіснуючий route)  
**Метод:** GET  
**Опис:** Відображається, коли користувач намагається отримати доступ до неіснуючої сторінки.

**Відповідь:**
- HTML сторінка з повідомленням про помилку
- Статус: 404 Not Found

---

#### 5. Сторінка помилки 500

**Route:** `/*` (при виникненні помилки сервера)  
**Метод:** GET  
**Опис:** Відображається при внутрішніх помилках сервера.

**Відповідь:**
- HTML сторінка з повідомленням про помилку
- Статус: 500 Internal Server Error

---

## API Endpoints

### Внутрішні API

Наразі проект не має власних REST API ендпоінтів. Вся логіка обробляється на рівні PHP шаблонів.

### Планується додати:

- `POST /api/auth/login` - Авторизація користувача
- `POST /api/auth/register` - Реєстрація користувача
- `GET /api/properties` - Отримання списку нерухомості
- `GET /api/properties/{id}` - Отримання деталей об'єкта
- `POST /api/properties` - Створення нового об'єкта
- `PUT /api/properties/{id}` - Оновлення об'єкта
- `DELETE /api/properties/{id}` - Видалення об'єкта

---

## Зовнішні API

### 1. Lorem Picsum API

**Endpoint:** `https://picsum.photos/300/200?random={timestamp}`  
**Метод:** GET  
**Опис:** Використовується для завантаження випадкових зображень на головній сторінці.

**Параметри:**
- `random` (query) - Timestamp для унікальності запиту

**Відповідь:**
- Зображення (image/jpeg)
- Статус: 200 OK

**Використання:**
```javascript
// У файлі scripts/script.js
const response = await axios.get('https://picsum.photos/300/200?random=' + Date.now(), {
    timeout: 5000,
    responseType: 'blob'
});
```

**Документація:** [https://picsum.photos/](https://picsum.photos/)

---

## Структура проекту

```
site/
├── index.php              # Головний роутер та точка входу
├── pages/                 # Шаблони сторінок
│   ├── home.php          # Головна сторінка
│   ├── login.php         # Сторінка входу/реєстрації
│   ├── 404.php           # Сторінка помилки 404
│   └── 500.php           # Сторінка помилки 500
├── scripts/              # JavaScript файли
│   └── script.js         # Основний скрипт з функціоналом
├── styles/               # CSS файли
│   ├── normalize.css
│   └── styles.css
└── images/              # Зображення
    ├── kv/              # Зображення квартир
    ├── rieltors/        # Фото ріелторів
    ├── zabudovniki/     # Логотипи забудовників
    └── agents/          # Логотипи агенцій
```

---

## Приклади використання

### JavaScript API виклики

#### 1. Завантаження випадкового зображення (Axios)

```javascript
async function fetchRandomImage() {
    try {
        const response = await axios.get(
            'https://picsum.photos/300/200?random=' + Date.now(),
            {
                timeout: 5000,
                responseType: 'blob'
            }
        );
        
        const img = document.createElement('img');
        img.src = URL.createObjectURL(response.data);
        // Додати img до DOM
    } catch (error) {
        console.error('Помилка завантаження:', error);
    }
}
```

#### 2. Реактивний пошук (Vue.js + Lodash)

```javascript
// Автоматична фільтрація карток нерухомості при введенні тексту
searchField.addEventListener('input', function() {
    const query = _.toLower(this.value);
    const properties = Array.from(document.querySelectorAll('.property-card'));
    
    const filtered = _.filter(properties, card => {
        const text = _.toLower(card.textContent);
        return _.includes(text, query);
    });
    
    properties.forEach(card => {
        card.style.display = _.includes(filtered, card) ? '' : 'none';
    });
});
```

#### 3. Порівняння фраз з використанням Set

```javascript
// Функція знаходить спільні слова між двома фразами
function comparePhrases(phrase1, phrase2) {
    const normalize = (text) => {
        return text
            .toLowerCase()
            .replace(/[.,!?;:()\[\]{}"'`«»]/g, ' ')
            .split(/\s+/)
            .filter(word => word.length > 0);
    };
    
    const set1 = new Set(normalize(phrase1));
    const set2 = new Set(normalize(phrase2));
    
    const common = [];
    for (const word of set1) {
        if (set2.has(word)) {
            common.push(word);
        }
    }
    
    return common;
}
```

---

## Технічні деталі

### Роутер

Роутер реалізований у файлі `index.php` як клас `Router`:

```php
class Router {
    private $routes = [];
    
    public function addRoute($path, $template, $title);
    public function getCurrentPath();
    public function dispatch();
}
```

### Обробка помилок

- **404 Not Found:** Автоматично обробляється роутером для неіснуючих routes
- **500 Internal Server Error:** Обробляється через try-catch блок у `index.php`

### Безпека

- Всі виводи користувацьких даних екрануються через `htmlspecialchars()`
- Використовуються параметризовані запити для роботи з БД (якщо є)
- XSS захист через фільтри та екранування

---

## Посилання на документацію

### Використані технології:

- **PHP:** [https://www.php.net/docs.php](https://www.php.net/docs.php)
- **Vue.js:** [https://vuejs.org/guide/](https://vuejs.org/guide/)
- **Axios:** [https://axios-http.com/docs/intro](https://axios-http.com/docs/intro)
- **Chart.js:** [https://www.chartjs.org/docs/latest/](https://www.chartjs.org/docs/latest/)
- **jQuery:** [https://api.jquery.com/](https://api.jquery.com/)
- **Lodash:** [https://lodash.com/docs/](https://lodash.com/docs/)
- **Moment.js:** [https://momentjs.com/docs/](https://momentjs.com/docs/)

### Зовнішні API:

- **Lorem Picsum:** [https://picsum.photos/](https://picsum.photos/)

---

## Контакти та підтримка

**Проект:** DIM.RIA  
**Розробник:** Єгор  
**Група:** КН-24  
**Університет:** Ельворті

---

